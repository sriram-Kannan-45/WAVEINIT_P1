# Vercel Routing Middleware uses this high-entropy secret to prevent the
# internal HTML self-fetch from being invoked by a public request.
# Generate a unique value with: openssl rand -hex 32
INTERNAL_RENDER_SECRET=
